<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.customerservice.mapper.AuditLogMapper">

    <resultMap id="auditLogMap" type="com.customerservice.model.entity.AuditLog">
        <id column="id" property="id"/>
        <result column="operator_type" property="operatorType"/>
        <result column="operator_id" property="operatorId"/>
        <result column="action" property="action"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="detail" property="detail"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cs_audit_log (operator_type, operator_id, action, target_type, target_id, detail, ip_address)
        VALUES (#{operatorType}, #{operatorId}, #{action}, #{targetType}, #{targetId}, #{detail}, #{ipAddress})
    </insert>

    <select id="selectByTarget" resultMap="auditLogMap">
        SELECT * FROM cs_audit_log WHERE target_type = #{targetType} AND target_id = #{targetId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByOperator" resultMap="auditLogMap">
        SELECT * FROM cs_audit_log WHERE operator_type = #{operatorType} AND operator_id = #{operatorId}
        ORDER BY created_at DESC
    </select>
</mapper>
